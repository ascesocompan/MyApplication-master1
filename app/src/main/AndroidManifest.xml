<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mac1.myapplication">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AscesoMA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MaestroLogginActivity" />
        <activity android:name=".ActivityRegistroMaestro" />
        <activity
            android:name=".activity_docente_menu"
            android:label="@string/title_activity_docente_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AlumnoLogginActivity" />
        <activity android:name=".ActivityRegistroAlumno" />
        <activity
            android:name=".activity_alumno_menu"
            android:label="@string/title_activity_alumno_menu"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>